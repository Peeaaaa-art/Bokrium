name: Scheduled Scaling
on:
  schedule:
    - cron: '0 16 * * *' # 日本時間 01:00：最小 0台（スリープ許可）
    - cron: '0 23 * * *' # 日本時間 08:00：最小 1台（常時起動）
  workflow_dispatch:

jobs:
  update_scale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master

      # 夜間：すべてのマシンの設定を「最小0台」に一括更新する
      - name: Night Mode (Sleep)
        run: |
          for id in $(fly machines list --json | jq -r '.[].id'); do
            fly machine update $id --metadata fly_proxy_min_machines_running=0 -y
          done
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      # 朝：すべてのマシンの設定を「最小1台」に戻す
      - name: Active Mode (Active)
        run: |
          for id in $(fly machines list --json | jq -r '.[].id'); do
            fly machine update $id --metadata fly_proxy_min_machines_running=1 -y
          done
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

