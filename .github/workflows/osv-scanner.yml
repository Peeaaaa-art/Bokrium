# OSV-Scanner: 依存関係の脆弱性スキャン（Gemfile / package.json 等）
# 結果は Security > Code scanning に表示される
# https://google.github.io/osv-scanner/github-action/
#
# This workflow uses actions that are not certified by GitHub.

name: OSV-Scanner

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  merge_group:
    branches: ["main"]
  schedule:
    - cron: "0 2 15 * *" # 毎月15日 02:00 UTC

# トップレベルは最小権限。security-events は再利用ワークフロー内で必要に応じて付与（Scorecard Token-Permissions 対応）
permissions:
  contents: read

jobs:
  scan-scheduled:
    if: ${{ github.event_name == 'push' || github.event_name == 'schedule' }}
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@2a387edfbe02a11d856b89172f6e978100177eb4
    with:
      scan-args: |-
        -r
        --skip-git
        ./

  scan-pr:
    if: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@2a387edfbe02a11d856b89172f6e978100177eb4
    with:
      scan-args: |-
        -r
        --skip-git
        ./
