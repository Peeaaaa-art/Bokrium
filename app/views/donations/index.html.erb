<div class="container my-5" data-controller="animation">

  <!-- ヒーロー（共通の導入文） -->
  <div class="text-center mb-5">
    <h1 class="rh3 fw-bold fade-in delay-1"><span class="fw-semibold big-shoulders-inline-bold fs-3" style="letter-spacing: 0.03em;">Bokrium</span> は広告なし・完全無料で運営しています。</h1>
    <p class="rh5 lead text-muted slide-in delay-2">
      Bokriumは、皆さまのご支援により成り立っています。<br>任意のご寄付やマンスリーサポート制度を通じて、応援いただけますと幸いです。
    </p>
  </div>

  <!-- 一度きりの寄付 -->
  <div class="row justify-content-center mb-5 fade-in delay-3">
    <div class="col-lg-8">
      <div class="text-center">
        <h2 class="rh4 fw-bold mb-3">一度きりの寄付</h2>
        <p class="text-muted mb-4">
          気軽に、自由な金額でご支援いただけます。
        </p>
      </div>
      <div class="d-flex justify-content-center">
        <div class="w-100" style="max-width: 400px;">
          <%= form_with url: donations_path, method: :post, local: true, data: { turbo: false } do %>
            <div class="mb-3">
              <%= number_field_tag :amount, nil,
                min: 100,
                class: "form-control text-center",
                placeholder: "金額（円）を入力" %>
            </div>
            <div class="d-grid">
              <%= submit_tag "寄付する", class: "btn btn-outline-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- マンスリーサポート -->
  <div class="row justify-content-center mb-5 fade-in delay-4">
    <div class="col-lg-8">
      <div class="text-center">
        <h2 class="rh4 fw-bold mb-3">🌱 マンスリーサポーター</h2>
        <p class="text-muted mb-4">
          月々300円から、継続的な応援ができます。
        </p>
        <%= link_to "月々300円で応援する", create_subscrption_path,
          class: "btn btn-lg btn-primary px-5 py-3 shadow rounded-pill fs-5" %>
        <p class="text-muted small mt-3">
          ※いつでも解約可能・支援は任意です。
        </p>
      </div>
    </div>
  </div>

</div>