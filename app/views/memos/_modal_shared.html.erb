<div class="modal fade modal-dialog-scrollable" id="memoEditModal"
     data-controller="modal-swipe"
     data-modal-swipe-target="modal"
     tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-xl modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-body">
        <%= form_with url: "#", method: :post, id: "memo-edit-form", data: { turbo_frame: "_top" } do |f| %>
          <div class="card-header d-flex justify-content-center py-2">
            <i class="bi bi-cloud-fog2-fill"></i>
            <i class="bi bi-cloud-haze2"></i>
            <i class="bi bi-cloud-fog2-fill"></i>
          </div>

          <%= hidden_field_tag "memo[content]", "", id: "memo_content_input" %>
          <div id="rich-editor-root" class="rich-editor-root"></div>
          <div class="d-flex justify-content-center">
            <%= f.submit "保存", class: "btn btn-primary my-3" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="confirmModal" tabindex="-1" aria-hidden="true"
     data-controller="confirm-modal"
     data-confirm-modal-target="modal">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-body text-center">
        <p>保存していない変更があります。どうしますか？</p>
        <button class="btn btn-secondary" data-bs-dismiss="modal">キャンセル</button>
        <button class="btn btn-danger" onclick="discardChanges()">破棄して閉じる</button>
        <button class="btn btn-primary" onclick="saveAndClose()">保存して閉じる</button>
      </div>
    </div>
  </div>
</div>