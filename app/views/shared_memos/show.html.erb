<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-header bg-rhodia-orange text-center">
          <h5 class="mb-0">📘 <%= @book.title %></h5>
          <p class="text-muted mb-0 small">by <%= @book.author.presence || "著者不明" %></p>
        </div>
        <div class="card-body">
          <div class="mb-4">
            <% if @book.book_cover_s3.attached? %>
              <%= image_tag @book.book_cover_s3, class: "img-fluid rounded", alt: @book.title %>
            <% elsif @book.book_cover.present? %>
              <img src="<%= @book.book_cover %>" class="img-fluid rounded" alt="表紙画像">
            <% end %>
          </div>

          <div class="rhodia-grid-bg px-3 py-3 rounded">
            <%= @memo.content&.html_safe.presence || "<p class='text-muted'>メモはまだありません。</p>".html_safe %>
          </div>

          <div class="mt-3 text-end text-muted small">
            <%= @memo.updated_at.strftime("%Y年%m月%d日 %H:%M") %> 更新
          </div>
        </div>
        <div class="card-footer d-flex align-items-center justify-content-between small text-muted">
          <span><i class="bi bi-person-circle me-1"></i><%= @user.name.presence || "匿名ユーザー" %></span>
          <span><i class="bi bi-link-45deg"></i> 共有メモ</span>
        </div>
      </div>
    </div>
  </div>
</div>