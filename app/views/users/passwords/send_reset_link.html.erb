<div class="container my-5" style="max-width: 720px;">
  <div class="card shadow-sm border-0">
    <div class="card-body px-4 py-5">
      <h2 class="mb-4 text-primary text-center fw-bold">パスワードの変更</h2>

      <p class="text-muted text-center mb-4">
        登録されたメールアドレス宛に、パスワード変更用のリンクを送信します。
      </p>

      <%= form_with model: current_user, url: trigger_reset_user_password_path, method: :post, local: true do |f| %>
        <div class="d-grid">
          <%= f.submit "変更リンクを送信", class: "btn btn-outline-secondary btn-lg shadow-sm" %>
        </div>
      <% end %>

      <div class="mt-4 text-center">
        <%= link_to "戻る", edit_user_registration_path, class: "btn btn-link text-decoration-none" %>
      </div>
    </div>
  </div>
</div>