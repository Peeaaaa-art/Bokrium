#!/usr/bin/env sh

set -eu

APP_ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"

exec env \
  RAILS_ENV=development \
  RACK_ENV=development \
  NODE_ENV=development \
  VITE_RUBY_ENV=development \
  "$APP_ROOT/bin/op-run" docker compose "$@"
